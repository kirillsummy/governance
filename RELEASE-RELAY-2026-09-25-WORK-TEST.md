# Эстафета: SUMMY work → test, 25.09.2026

**Состояние:** предложение, не действует до команды владельца в назначенном
чате с полным SHA коммита Governance, содержащего этот файл.

**Назначенная сессия:** Codex «CRM брак», thread
`01a0c997-4472-7110-9c48-1a6f12a6ee31`, GitHub-исполнитель
`@523uran523`. Другие сессии не наследуют полномочие.

**Среда и срок:** только защищённые ветки `test` на GitHub; до
26.09.2026 23:59 Europe/Moscow. Развёртывание приложения, миграции БД,
внешние финансовые вызовы, отправка сообщений, теги и production этой
эстафетой не разрешаются.

| Репозиторий | Источник `work`, полный SHA | Текущая цель `test` до переноса и цель возврата |
|---|---|---|
| backend | `ca369dbb077eb41ef06436eddcf5f9f4db7c228a` | `0a66210944eecccbacfab0cf12317561e7ed44e4` |
| CRM | `8238dbb4f5e2b5ce2236d526e4a77c67e5d334dc` | `bf7ac8ad0888454fa461686eceaa17b6a6c3dd5c` |
| master-app | `473f635bed5df07eed659a3c86bd31f7f1b23738` | `804c0f130615eb138945d41d58ac3004e6fe4ae0` |

`client-app/test` уже на один коммит впереди `work`, `website/work=test`.
Эти ветки не менять и не объявлять заново перенесёнными. Архивные
`singular` не удалять.

**Разрешённое действие после активации:** независимо принять diff и CI,
сверить фактические remote refs и состав открытых PR; затем обычным merge без
переписывания истории перенести указанные источники в соответствующие
`test`. Порядок: backend → CRM → master-app. Если источник или цель сдвинулись,
проверка красная, появился контрактный конфликт или изменился состав,
остановиться и оформить новый файл/разрешение; этот документ нельзя
молча обновить после активации.

**Цель возврата:** сохранить достижимыми три исходных SHA `test` из таблицы.
При проблеме после merge не делать `reset` или force push: остановить
дальнейший перенос и согласовать revert конкретного merge-коммита либо
новый исправляющий коммит; TEST-БД и стенд этой эстафетой не затрагиваются.
Перед любым отдельным развёртыванием потребуется проверка доверенного SSH
host key, реального deployed SHA, `alembic current`, роли мигратора,
исторических данных/конфликтов 0139, резервной копии, филиальных grants и
новая явная команда владельца. Наличие кода `0139` в Git не доказывает
применение миграции к TEST-БД.

**Основание проверок:** `backend/work` — 2036 тестов на новой изолированной
PostgreSQL до merge уникального test-коммита, 36 адресных тестов после,
Ruff/mypy/API/schema contracts и синтетическая миграционная репетиция;
`crm/work` — 1596 полных тестов до последних адресных правок, затем 49
адресных, typecheck/lint/build; `master-app/work` — 46 BFF и 153 UI-теста,
typecheck/build. Ничего из этого не является TEST-приёмкой с реальными данными.
Черновой [CRM PR #187](https://github.com/kirillsummy/crm/pull/187) открыт.

**Активация:** владелец пишет назначенной сессии: «Активирую эстафету
`<полный SHA governance-коммита с этим файлом>` для merge перечисленных
`work` в `test`». До этого запись в governance — только описание плана.
