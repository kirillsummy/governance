# Карта: где что искать

Первая дверь теперь — [AGENTS.md](AGENTS.md), затем [состояние](docs/current-state.md)
и [архитектура](docs/architecture.md). Ниже сохранён прежний навигатор с историческими
заметками, чтобы не потерять основания решений. Единый DOC разработки — этот репозиторий;
упоминания отдельного `docs` ниже означают сохранённую прежнюю базу знаний.

## Вопрос → документ

| Вопрос | Куда |
|---|---|
| Кто есть кто, как устроен процесс, релизы, запреты | [`CHARTER.md`](CHARTER.md) |
| Кому писать, как передаётся работа | [`roles/TEAM.md`](roles/TEAM.md) |
| Что значит слово, которое все говорят по-разному | [`GLOSSARY.md`](GLOSSARY.md) |
| Почему экосистема устроена так | [`decisions/`](decisions/) |
| Правило, которое читают два и более продукта | [`contracts/`](contracts/) |
| Почему **внутри продукта** сделано так | `<продукт>/docs/decisions/` |
| Как устроен продукт | `<продукт>/AGENTS.md`, затем его `docs/` |
| Как считаются деньги, как работаем с людьми | репозиторий `docs`, раздел «дело» |
| Подходы и внутреннее устройство системы | репозиторий `docs`, раздел «система» |
| На что уже наступали, где остановились | репозиторий `context`, память сессий |
| Что лежит в рабочей папке и чем её обслуживать | репозиторий `workspace`, `README.md` |

## Прежнее распределение четырёх слоёв (история до DOC 19.09.2026)

Сверху вниз, конфликт решает верхний:

1. **Канон** — этот репозиторий: устав, решения экосистемы, контракты.
   Обязателен для всех, меняется веткой с ревью архитектора.
2. **Продукты** — код, решения продукта, его устройство. Правда о поведении —
   код и тесты.
3. **Знание** — репозиторий `docs`: как устроено дело и как устроена система.
   Живёт дольше кода, читается людьми.
4. **Память** — репозиторий `context`: опыт сессий и грабли. Подсказывает, но
   полномочий не даёт: в цепочке разрешения конфликтов из устава её нет.

Правило принадлежности: **зависит ли от этого кто-то снаружи.** Зависит другой
продукт — в канон. Живёт внутри одного дома — в продукт, рядом с кодом.
Переживёт переписывание кода — в знание. Опыт сессии — в память.

## Продукты

| Репозиторий | Что это | Домен |
|---|---|---|
| `website` | публичный сайт | `summy.ru` |
| `crm` | управленка: процессы, зарплата, склад, клиенты | `admin.summy.ru` |
| `master-app` | приложение мастера: клиент `web/` + прослойка `bff/` | `master.summy.ru` |
| `client-app` | клиентское приложение — код в feature, см. docs/current-state.md | — |
| `backend` | данные и вся продуктовая логика | внутренний |

Служебные: `governance` — канон, `docs` — знание, `context` — память,
`workspace` — рабочая папка, `data` — исторические исходники схемы и импортёров;
действующий журнал миграций находится в backend.

**Исторический замер 16.08.2026, числа не обновлялись по live-БД.**

**Схема данных принадлежит `backend`.** Её ведёт один журнал — alembic, 139
таблиц, и первая ревизия так и называется: `0001_baseline.sql`. Одиннадцать
SQL-файлов в `data` — это исходники, с которых снят тот слепок: все 56 их
таблиц входят в 139 и ничего уникального там нет.

Проверено 16.08.2026 сравнением множеств. До этой проверки канон утверждал
обратное — что журналов два и они конкурируют. Утверждение было неверным:
его сделали по виду папок, не открыв ни одной ревизии.

## Снимок и живое состояние

В новых docs хранится датированный снимок с SHA, а не обещание живого состояния.
Какая версия на бою, на какой ветке checkout и что сейчас
чинится — это спрашивают у системы, а не у документа: прод сообщает версию
сам, состояние ветки показывает Git. Документ, повторяющий живое состояние,
устаревает молча и начинает врать.

## Где визиты — не там, где написано на двери

> Историческое наблюдение 18.08.2026: числа ниже не проверены 19.09.2026.
> За новой моделью записи/заказа см. [database](docs/database.md).

⚠️ **В том замере таблица `summy_data.visits` пуста.** Ноль строк, и это не поломка — визиты
никогда там и не жили.

Считать визиты нужно по:

- `appointments` — записи, 20 892 строки;
- `client_visit_summary` — свод по клиенту;
- `contract_v1_client_visits` — витрина для читателей, 18 626 строк.

Заведено 18.08.2026: управляющий операционной команды пошёл считать визиты,
спросил `visits` и получил **честный ноль на не тот вопрос**. Имя таблицы
обещало содержимое, которого в ней нет, — и по имени это неотличимо от «визитов
не было».
